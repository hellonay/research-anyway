사진 업로드 시 서버 액션 요청 크기 상한을 1MB→4mb로 올렸지만(`next.config.ts`), 이는 Next.js 자체 기본값 완화일 뿐이고 Vercel 서버리스 함수의 요청 본문 상한(4.5MB, 설정으로 변경 불가)은 여전히 남아있다. 고화질 사진을 한 번에 여러 장 올리면 여전히 실패할 수 있으며, 근본 해법은 `docs/decisions/data-storage.md`에 이미 적힌 대로 사진을 별도 오브젝트 스토리지(Vercel Blob 등)로 분리해 클라이언트가 직접 업로드하게 하는 것이다.
